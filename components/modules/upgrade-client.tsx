"use client";
import Link from "next/link";
import { CheckCircle2, Sparkles } from "lucide-react";
import { Button } from "@/components/ui/button";import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";import { Badge } from "@/components/ui/badge";import { useAuth } from "@/hooks/use-auth";import { ActionToast } from "@/components/forms/action-toast";import { useState } from "react";
const features = ["Unlimited Projects","Unlimited Team Members","Offline Sync","Proposal Builder","Contracts","Invoices","Advanced Reports","Analytics","AI Assistant","RFI Management","Daily Logs","Scheduling","Advanced Blueprint Tools"];
export function UpgradeClient(){const {user,upgrade,downgrade}=useAuth(); const [toast,setToast]=useState(""); return <div className="mx-auto max-w-5xl space-y-8"><section className="rounded-[2rem] border bg-card p-8 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-blue-600 text-white"><Sparkles className="h-6 w-6"/></div><h1 className="mt-5 text-4xl font-black tracking-tight">Upgrade to Groundbreak Premium</h1><p className="mx-auto mt-3 max-w-2xl text-muted-foreground">Unlock the complete construction operating system. Premium badges disappear immediately after upgrade in this mock subscription flow.</p><Badge className="mt-5" tone={user.plan==="premium"?"green":"blue"}>Current plan: {user.plan}</Badge></section><div className="grid gap-6 md:grid-cols-2"><Card><CardHeader><CardTitle>Free</CardTitle><CardDescription>For homeowner previews or a single small project.</CardDescription></CardHeader><p className="text-4xl font-black">$0</p><ul className="mt-6 space-y-3 text-sm text-muted-foreground"><li>• 1 active project</li><li>• 2 blueprint uploads</li><li>• Basic budget</li><li>• Messages and approvals</li></ul><Button onClick={()=>{downgrade();setToast("Plan changed to Free.")}} className="mt-6 w-full" variant="outline">Use Free</Button></Card><Card className="border-blue-300 bg-blue-500/5"><CardHeader><CardTitle>Premium</CardTitle><CardDescription>For contractors and construction teams.</CardDescription></CardHeader><p className="text-4xl font-black">$499<span className="text-sm text-muted-foreground">/mo</span></p><ul className="mt-6 grid gap-3 text-sm text-muted-foreground sm:grid-cols-2">{features.map(f=><li key={f} className="flex gap-2"><CheckCircle2 className="h-4 w-4 text-emerald-500"/>{f}</li>)}</ul><Button onClick={()=>{upgrade();setToast("Premium unlocked. All premium modules are now accessible.")}} className="mt-6 w-full" variant="secondary">Upgrade with Stripe mock</Button></Card></div><div className="text-center"><Button asChild variant="outline"><Link href="/dashboard">Return to dashboard</Link></Button></div>{toast&&<ActionToast message={toast} onClose={()=>setToast("")}/>}</div>}

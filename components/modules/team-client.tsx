"use client";
import { useMemo, useState } from "react";
import { Mail, Phone, Plus, Search, Shield, Trash2, UserCog } from "lucide-react";
import { teamMembers as seed } from "@/data/mock";
import { TeamMember } from "@/types";
import { Button } from "@/components/ui/button";import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";import { Badge } from "@/components/ui/badge";import { Input } from "@/components/ui/input";import { ActionToast } from "@/components/forms/action-toast";
export function TeamClient(){const [items,setItems]=useState<TeamMember[]>(seed); const [q,setQ]=useState(""); const [toast,setToast]=useState(""); const filtered=useMemo(()=>items.filter(m=>[m.name,m.email,m.role,m.company].join(" ").toLowerCase().includes(q.toLowerCase())),[items,q]); const add=()=>{setItems([{id:`tm${Date.now()}`,name:"New Team Member",email:"invite@groundbreak.tech",phone:"+1 (555) 000-0000",jobTitle:"Invited collaborator",company:"Atlas Build Co.",role:"Viewer",activeProjects:["Palm Residence"],status:"Invited",photo:"https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?auto=format&fit=crop&w=300&q=80"},...items]);setToast("Invitation created and permission profile assigned.")}; const remove=(id:string)=>{setItems(items.filter(m=>m.id!==id));setToast("Member removed from workspace.")}; const promote=(id:string)=>{setItems(items.map(m=>m.id===id?{...m,role:"Project Manager",status:"Active"}:m));setToast("Role changed to Project Manager.")}; return <div className="space-y-6"><section className="rounded-[2rem] border bg-card p-6"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><Badge tone="green">Team directory</Badge><h1 className="mt-3 text-4xl font-black">Team Management</h1><p className="mt-2 max-w-3xl text-muted-foreground">Add members, assign projects, manage permissions, and keep contractor, architect, engineer, homeowner, and admin access organized.</p></div><Button onClick={add} variant="secondary"><Plus className="h-4 w-4"/>Add member</Button></div></section><Input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search members, roles, companies..."/><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{filtered.map(m=><Card key={m.id}><CardHeader><div className="flex items-center gap-3"><img src={m.photo} alt={m.name} className="h-14 w-14 rounded-2xl object-cover"/><div><CardTitle>{m.name}</CardTitle><CardDescription>{m.jobTitle}</CardDescription></div></div><Badge tone={m.status==="Active"?"green":"orange"}>{m.status}</Badge></CardHeader><div className="space-y-2 text-sm text-muted-foreground"><p className="flex gap-2"><Mail className="h-4 w-4"/>{m.email}</p><p className="flex gap-2"><Phone className="h-4 w-4"/>{m.phone}</p><p className="flex gap-2"><Shield className="h-4 w-4"/>{m.role} · {m.company}</p></div><div className="mt-4 flex flex-wrap gap-2">{m.activeProjects.map(p=><Badge key={p} tone="blue">{p}</Badge>)}</div><div className="mt-5 flex flex-wrap gap-2"><Button size="sm" variant="outline" onClick={()=>promote(m.id)}><UserCog className="h-4 w-4"/>Edit role</Button><Button size="sm" variant="ghost" onClick={()=>remove(m.id)}><Trash2 className="h-4 w-4"/>Remove</Button></div></Card>)}</div>{toast&&<ActionToast message={toast} onClose={()=>setToast("")}/>}</div>}
